<%* tR = ""; const refTags = new Set(); const templateFolder = "templates"; const files = app.vault.getMarkdownFiles(); for (const file of files) { if (file.path.startsWith(templateFolder)) continue; const cache = app.metadataCache.getFileCache(file); if (cache && cache.tags) { for (const t of cache.tags) { if (t.tag.startsWith("#ref/")) { refTags.add(t.tag.substring(5)); } } } } const tagList = [...refTags].sort(); const options = ["＋ 新しいタスクを作成", ...tagList]; const selected = await tp.system.suggester(options, options, false, "タスクを選択"); if (selected) { let taskName = selected; if (selected === "＋ 新しいタスクを作成") { taskName = await tp.system.prompt("新しいタスク名を入力"); } if (taskName) { tR = "\n### #ref/" + taskName + "\n" + "⏱ 開始 " + tp.date.now("HH:mm") + "\n"; } } _%>
